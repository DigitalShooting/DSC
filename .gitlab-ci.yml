stages:
  - setup
  - test

cache:
  paths:
  - node_modules/
  - disziplinen/

setup:
  stage: setup
  image: node:13
  script:
    - npm install
    - git submodule init
    - git submodule update

test:
  stage: test
  image: node:13
  script:
    - npm run test
