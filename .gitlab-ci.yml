stages:
  - build
  - test

cache:
  paths:
  - node_modules/
  - disziplinen/

build:
  stage: build
  image: node:13
  script:
    - ls -lah
    - git submodule update --checkout --init
    - npm install

test:
  stage: test
  image: node:13
  script:
    - npm run test
